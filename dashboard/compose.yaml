services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=rest_simulator'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=rest'
    ports:
      - '5432:5432'
